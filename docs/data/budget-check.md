# Generated budget check

Using the BOM's **typical** values for required/recommended `both` + `collins` rows,
excluding optional/alternative branch items, the additive planning total is:

**$20,529 USD**

This is a planning check, not a quote. It intentionally includes allowances for fabrication,
miscellaneous hardware, tuning and alignment. Review `bom.csv` row-by-row before using it
as a purchasing list.

## Sources

| Claim | Source |
| --- | --- |
| The generated planning total is derived from the machine-readable BOM's typical values. | [Repository BOM](bom.csv) |
